# Ansible runs ON ms02 (local connection). From Mac:
#   ssh ms02 'cd ~/Workspace/microscaler/shared-gitops-k8s-cluster && just cluster-edge-apply'
all:
  hosts:
    ms02:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3

# Helm overlay for official imgproxy chart.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data

configMapGenerator:
  - name: imgproxy-helm-values
    files:
      - values.yaml=helm-values.yaml

generatorOptions:
  disableNameSuffixHash: true
  immutable: false

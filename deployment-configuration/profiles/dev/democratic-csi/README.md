# democratic-csi secrets (dev)

Canonical (only) path:

`deployment-configuration/profiles/dev/democratic-csi/`

| File | Cluster Secret |
|------|----------------|
| `driver-config.secret.yaml` | `democratic-csi-driver-config` |
| `ssh-key.secret.yaml` | `democratic-csi-ssh-key` |

Flux: `profile-secrets-democratic-csi`.

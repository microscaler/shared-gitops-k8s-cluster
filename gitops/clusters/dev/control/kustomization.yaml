# Dev control — GitOpsSets with images mirrored to ms02 registry.
# 1) gitopssets-controller HelmRelease (CRDs + controller)
# 2) platform-gitopssets (GitOpsSet CRs) after dependsOn Ready
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gitopssets-controller-ks.yaml
  - platform-gitopssets-ks.yaml

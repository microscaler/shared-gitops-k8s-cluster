apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepository.yaml
  - helm-release.yaml
  - envoyproxy.yaml
  - gatewayclass.yaml
  - gateway.yaml
  - referencegrant-tls.yaml
  # loadlinker/hauliage hosts: product HTTPRoute (hauliage/k8s/frontend/httproute.yaml)
  - httproutes/opensearch.yaml
  - httproutes/platform-uis.yaml
  # Tilt UIs stay on ms02 haproxy (outside Envoy) — see config/lan-http-vhosts.yaml
  - tcproutes/platform-l4.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - script-configmap.yaml
  - cronjob.yaml
  # One-shot bootstrap: `just postgres-backup-now` (Job names are immutable; do not GitOps them)


apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - storageclass-local-path-retain.yaml
  - helmrepository.yaml
  # Config + secrets: deployment-configuration/profiles/dev/postgres-ha (Flux profile-config)
  - helm-release.yaml
  - postgres-lb-service.yaml
  - pgpool-exporter.yaml

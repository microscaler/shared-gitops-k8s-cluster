apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Config: deployment-configuration/profiles/dev/messaging (Flux profile-config)
  - mailpit-storage.yaml
  - helmrepository.yaml
  - helm-release-mailpit.yaml
  - service-mailpit.yaml
  - inbucket.yaml
  # MailHog retired — use Mailpit (LB .235).

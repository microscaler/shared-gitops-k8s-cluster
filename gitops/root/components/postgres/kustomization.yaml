apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - storageclass-local-path-retain.yaml
  - gitrepository.yaml
  # Config + secrets: deployment-configuration/profiles/dev/postgres (Flux profile-config)
  - helm-release.yaml

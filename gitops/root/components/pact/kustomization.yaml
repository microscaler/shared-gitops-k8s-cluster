apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Config + secrets: deployment-configuration/profiles/dev/pact (Flux profile-config)
  - storage.yaml
  - pact-postgres.yaml
  - pact-broker.yaml

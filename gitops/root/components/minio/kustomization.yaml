apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepository.yaml
  - claim.yaml
  # Config + secrets: deployment-configuration/profiles/dev/minio (Flux profile-config)
  - helm-release.yaml
